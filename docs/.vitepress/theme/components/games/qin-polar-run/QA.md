# Northern Run 验证记录

日期：2026-09-07。工作目录：WSL Debian /home/deve/gitclone/Develata.github.io。
浏览器：Windows Chrome 151.0.7922.172，由 Playwright 控制独立无头浏览器。
未使用用户浏览器配置。以下为初次实现的本地记录，推送前自审复测见末节。

## 构建和静态检查

| 项目 | 实际结果 |
| --- | --- |
| cargo test --locked | 12 项通过，最终构建前运行耗时约 0.41s |
| cargo clippy --locked --all-targets -- -D warnings | 通过 |
| cargo fmt | 已运行；最终格式检查通过 |
| npm run game:qin-polar-run:wasm | 通过：测试、release wasm32 构建、web 绑定生成 |
| npm run build | 通过；最后一次完整生产构建 43.58s |
| TypeScript strict 检查 | 游戏输入/渲染/配置通过 |
| vue-tsc strict 检查 | GameEntry.vue 及其依赖通过；临时工具 TS 5.9.3 / vue-tsc 3.1.8，没有添加项目依赖 |
| git diff --check | 通过 |
| 导入/依赖检查 | 无其他游戏内部实现、共享游戏引擎、外部资源、物理引擎、持久化或随机渲染逻辑 |
| 文件长度 | 新实现文件均少于 400 行，最长为 320 行的样式文件 |

Rust 测试包含：换道边界/连续位置、解析跳跃/落地、俯身期限/动作互斥、车道和动作碰撞、
实际纵向碰撞窗口、两次受撞与免疫、拾取加分与重置、同种子同输入重放、非法/过密路径拒绝、
无效 dt/暂停/高刷新率输入，以及一百万排障碍的跨段可达性验证。
另有 8 个种子、每个 300 秒的真实步进安全路线测试，使用 Player 换道和实际碰撞逻辑，检查不受撞及槽位有界。

生成产物：qin_polar_run_bg.wasm = **22,205 bytes**；JS = **7,361 bytes**。
无 wasm-pack / wasm-opt / 图形引擎依赖；Cargo 锁定的依赖仅为 wasm-bindgen 及其小型工具依赖。

## 浏览器实际操作

| 范围 | 检查与结果 |
| --- | --- |
| 桌面 | 1440×900、1440×810、1024×720；三道可见，标题/开始按钮/积分/暂停/结束面板可读 |
| 键盘 | 左右方向键、A/D、W、上箭头、Space、S、下箭头逐一触发并读取实际 x/y/duck 结果 |
| 暂停 | Escape 暂停和恢复；暂停按钮、继续、重新开始均通过；暂停时分数不增长 |
| 竖屏 | 390×844，触摸设备，设备 DPR=3；原生 CDP touchStart/move/end 实测四向滑动 |
| 手势拒绝 | 短滑和近对角滑动不换道；游戏滑动未造成页面滚动 |
| 横屏 | 844×390；最终生产页 scrollWidth=844、scrollHeight=390，无白边或水平溢出 |
| 第一撞 | 实测仍 running，hits=1，短时免疫约 1.925s，减速至约 8.59m/s |
| 第二撞 | 实测 over，hits=2，显示本局分数及再来一次，游戏 RAF=0 |
| 连续运行 | 通过真实方向键跟随安全路线，最终运行至 62.05 游戏秒、零受撞；中间暂停一次用于记录 |
| 生产包 | 预览产物中开始、换道、跳跃、暂停、重开可用，未捕获 pageerror |
| 懒加载 | 生产首页和 Game Lab 的网络记录均无本游戏 JS/WASM 请求；点击游戏后才请求 JS 和 WASM |

玩法图形已人工查看截图：秦式冠服、白熊口鼻/耳朵/四肢可识别；三道路线、冰岩、低墙和高门梁轮廓有区别。
生产桌面、竖屏及横屏截图保存在本任务的本地输出目录；不作为网站运行资源。

## 生命周期与失败路径

在页面加载前对本游戏的 RAF 和事件注册做计数，只观察并转发原生调用：

| 状态 | 游戏 RAF | 游戏监听器 | 游戏画布 |
| --- | ---: | ---: | ---: |
| 正在运行 | 1 | 11 | 1 |
| 暂停/结束 | 0 | 11 | 1 |
| 连续重开两次 | 1 | 11 | 1 |
| 导航离开到 Game Lab | 0 | 0 | 0 |
| 返回后再玩 | 1 | 11 | 1 |

生产包另观察到离开页面调用 Runner.free()，WASM 内存重新进入后没有增加。
模拟 document.hidden=true + visibilitychange：暂停且 RAF=0；恢复可见性后保持暂停，点击继续后时间正常推进。
无头 Chrome 标签切换不能可靠模拟真实浏览器后台隐藏，因此这里明确报告为事件模拟测试。

阻断生成 JS 请求：显示中文错误面板；解除阻断、点击重新加载后恢复。
用 WEBGL_lose_context 扩展制造上下文丢失：显示中文错误面板；重新加载恢复一个画布。
异步加载的卸载保护及 observer/material/geometry/core 的释放路径另做源码自审。

## 成本与 CI 核验

最终实现样本：111 场景节点、6 个上传几何体、0 张纹理；桌面约 96–100 次绘制调用，
手机竖屏样本 95 次。竖屏 DPR 实测封顶 1.25。上述数量在重开和路线推进时保持有界。
优化前为 976 节点、密集画面约 173 次调用；障碍/路面实例化去除了该结构性开销。
不把桌面触摸模拟称为真实 Android GPU 性能验收，也未声称某个测得帧率。

每帧 576 字节帧缓冲，一次粗粒度 WASM 调用，约 1.1KiB 双向复制。
热路径没有新建 Three.js 对象、数组或材质；生成绑定仍有小型临时切片分配，未声称零分配。

CI 变更自审确认：只在精确输出缓存缺失时安装 Rust、测试和构建；命中时直接做 Node/VitePress 构建。
新闻路径不进入 Rust 缓存键；没有重复 Rust 工作流；原 Pages 权限/并发/环境/部署 action 保留。
本地已成功从源码生成包并用生产站点消费；尚未提交远端，未声称 GitHub Actions 实际发生过缓存命中。

## 边界与未覆盖项

- 首次玩家的 1–3 分钟存活时长仍是设计目标，未进行新玩家统计。
- 未使用真实 Android/iOS 设备或独立 GPU 性能分析器。
- 页面隐藏用事件模拟覆盖；真实后台标签的浏览器节流仍可后续做真机检查。
- 未执行线上 Pages 部署；首次发布需验证远端缓存与静态资源服务。

## 推送前自审补充（2026-09-07）

已将实现基线从 66771f5 快进到远端 41e954d；保留新增认知训练游戏、其 CI 检查和独立异步加载器。
页面改用上游 injectTitle:false，不再用 CSS 隐藏自动标题。

自审复现并修复两个 P2 生命周期问题：

1. WebGL 失败后重试使实际 WASM memory 实例数由 1 增为 2。loadWasm.ts 现在复用已加载模块并合并并发初始化；仅失败的 JS 下载变更 URL。
2. 模拟 ResizeObserver 初始化抛错后遗留 1 个画布。RunnerScene 现在对整个初始化路径捕获异常并释放部分构造的资源。

新增 4 个 Node 内置回归测试，覆盖重试复用、并发初始化、JS 下载失败恢复和 WASM 初始化失败恢复，全部通过。
合入远端后的首次本地全站构建碰到约 2GiB Node 默认堆上限；改用单次构建进程的 4GiB 堆配额验证，不修改全局配置。

复测结果：

- 完整生产构建通过，51.71s；仅保留已有 sshconfig 高亮回退提示。
- 12 个 Rust 测试、WASM release 构建、cargo fmt/clippy、Vue strict 检查及上游认知训练测试通过。
- 连续 3 次 WebGL 丢失与恢复：每次均为 1 个 WASM memory、1 个游戏画布。
- 故意使 ResizeObserver 构造抛错：错误面板下游戏画布为 0；恢复后为 1，WASM memory 仍为 1。
- 开始、键盘动作、暂停、重开及返回 Game Lab 通过；离开后游戏画布为 0，返回仍复用同一 WASM memory。Game Lab 自身背景画布另计。
- 新建浏览器上下文打开 Game Lab：游戏 JS/WASM 请求为 0；进入游戏后才加载。
- 1200×800 桌面画面与 390×844 触摸布局已查看；手机视口无水平或垂直溢出，暂停/继续可用。页面仅有一个游戏标题。
- 故障注入以外未捕获页面异常。线上部署及真实移动设备不在这份本地验证结论内。

## P2 / P3 修复回归（2026-09-07）

- P2：游戏局部 CSS 保留四侧安全区，窄屏只调整额外 HUD 边距；开始、暂停、反馈和操作提示均随安全区布局。
- 390×844，模拟 top=59/bottom=34：SCORE 与暂停按钮 top=75（原为 16），开始区底边 790，操作提示底边 788，均避开底部安全区。
- 844×390，模拟 left=right=59/bottom=21：SCORE left=83，暂停按钮 right edge=761；无横向或纵向溢出。1200×800、零安全区时 HUD 仍为 24px 边距。
- P3：Rust 以原 0.16s 时长计算 smoothstep 权威横向位置，渲染和碰撞共用它，无第二套视觉延迟或新增依赖。
- 新增双向缓动回归，验证单调、有界、对称、准确结束、返回中道，以及过渡中两车道碰撞。13 个 Rust 测试全部通过（含百万排可达性与 8×300 秒实际安全路线）；clippy -D warnings 通过。
- 生产 WASM 实测 x 随时间 0.04167/0.08333/0.125/0.16667s 为 0.16813/0.53123/0.87738/1.0；固定步采样准确到道。WASM 大小 22229 bytes。
- npm run game:qin-polar-run:wasm 通过；全站生产构建通过，58.17s（本地进程 4GiB Node 堆）。
- 生产预览实测原生触摸左滑到左道、方向键回中道、Space 跳跃、暂停和重开通过；无 pageerror。手机安全区使用 Chrome/CDP 模拟，仍不作为真机性能结论。
